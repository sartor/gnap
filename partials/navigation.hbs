<ul class="nav" id="nav" role="menu">

</ul>

<ul class="navbar-nav mr-auto">
    {{#foreach navigation}}
        <li class="nav-item nav-{{slug}}{{#if current}} active{{/if}}" role="menuitem">
            <a href="{{url absolute="true"}}" class="nav-link">{{label}}{{#if current}} <span class="sr-only">(current)</span>{{/if}}</a>
        </li>
    {{/foreach}}
</ul>